import { Helmet } from 'react-helmet';
import { Box, Divider, SEO, Text } from '../views/components';
import AuthorList from '../views/pages/Authors/AuthorList';

import authors from '../../content/author.yaml';

<!-- import YAML from 'yamljs';
const authors = YAML.parse(authorData); -->

<SEO pageTitle="নির্দেশনা" />
<Box maxWidth={960} margin="0 auto" padding={{ xs: 3, sm: 4 }}>

<Text variant="h2" textAlign="center">
  লেখালেখির নিয়মনীতি
</Text>
<Divider />

বিস্তারিত শীঘ্রই জানানো হবে :)

### গিট কমিট মেসেজ লেখার নিয়ম

আপনি যদি সরাসরি আমাদের গিট প্রজেক্টে কন্ট্রিবিউট করতে চান তবে কমিট ম্যাসেজ লেখার ক্ষেত্রে নিচের নিয়মগুলো মনে রাখার চেষ্টা করবেন। কমিট ম্যাসেজে ইমোজি ব্যবহারকে আমরা অনেক গুরুত্ব দিয়ে থাকি।

```
➕ Add new post (কিছু যোগ করলে)

🐛 Fix post spelling (কিছু ঠিক করলে)

🗑 Remove extra spacing (কিছু বাদ দিলে)

♻️ Refactor post summary card (কিছু রিফ্যাক্টর করলে)

▶️ Start automatic post formatting feature (কিছু চালু করলে)

⏹️ Stop automatic post formatting feature (কিছু থামিয়ে দিলে)

📄 Reformat post code example (কিছু রিফরম্যাট করলে)

📦 Build sqlite package for windows (কিছু বিল্ড করলে)

↕️ Update dependencies (কিছু আপডেট করলে)

🔖 Release v1.0.0 (কিছু রিলিজ করলে)

📝 Document contribution guide (কোন নির্দেশনা যোগ করলে)

⚡️ Optimize build process (কিছু অপটিমাইজ করলে)
```

</Box>
